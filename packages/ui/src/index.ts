/**
 * Shared React components used by `apps/web`.
 *
 * M1 skeleton: empty. Components land in M3 when annotation UI + dashboards
 * begin.
 */

export const PLACEHOLDER = '@llm-judge/ui placeholder';
