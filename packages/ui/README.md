# @llm-judge/ui

Shared React components consumed by `apps/web`. Will host shadcn/ui + Tremor primitives + project-specific composites (trace viewer, score chips, run-diff cells).

M1 status: skeleton.
