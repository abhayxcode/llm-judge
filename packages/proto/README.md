# @llm-judge/proto

Shared TypeScript types — used by `apps/web`, `@llm-judge/sdk`, and any future TS consumer. Source of truth lives here so trace + score schemas don't drift between client + server.

M1 status: skeleton with branded `ULID` and `ISO8601` types only. Real schemas land alongside CH + PG migrations.
